name: anime Dito
description: Anime browser powered by the Jikan API
publish_to: none
version: alpha.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  url_launcher: ^6.2.5
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
